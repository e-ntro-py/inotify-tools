[workspace]
resolver = "2"
members = ["libinotifytools", "src"]

# Package metadata is repeated per member (no workspace inheritance) for
# Rust 1.63.  The real version comes from packaging/version.sh.

[profile.release]
panic = "abort"
lto = true
codegen-units = 1

[profile.dev]
panic = "abort"
